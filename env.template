# =============================================================================
# MOMENTUM - Environment Variables Configuration Template
# =============================================================================
# 
# INSTRUCTIONS:
# 1. Copy this file to .env: cp env.template .env
# 2. Fill in all the values below with your actual credentials
# 3. Never commit .env to git (it's in .gitignore)
#
# =============================================================================

# =============================================================================
# FIREBASE CLIENT CONFIGURATION (Browser-side)
# =============================================================================
# These values are available in the browser and are mapped from MOMENTUM_ prefix
# Get these from: Firebase Console > Project Settings > General > Your apps > Web app
# =============================================================================

# Firebase API Key
MOMENTUM_NEXT_PUBLIC_FIREBASE_API_KEY="AIza..."

# Firebase Auth Domain (usually: your-project-id.firebaseapp.com)
MOMENTUM_NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="your-project-id.firebaseapp.com"

# Firebase Project ID
MOMENTUM_NEXT_PUBLIC_FIREBASE_PROJECT_ID="your-project-id"

# Firebase Storage Bucket (usually: your-project-id.appspot.com)
MOMENTUM_NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="your-project-id.appspot.com"

# Firebase Messaging Sender ID
MOMENTUM_NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="..."

# Firebase App ID
MOMENTUM_NEXT_PUBLIC_FIREBASE_APP_ID="1:..."

# =============================================================================
# FIREBASE ADMIN CONFIGURATION (Server-side)
# =============================================================================
# Required for server-side operations (Firestore, Auth, Storage)
# Get this from: Google Cloud Console > IAM & Admin > Service Accounts
# Create a service account with Firebase Admin role, then download JSON key
# =============================================================================

# Firebase Admin Service Account JSON (entire JSON as a single-line string)
# IMPORTANT: Paste the ENTIRE JSON content here, enclosed in single quotes
# The JSON should include: type, project_id, private_key_id, private_key, client_email, etc.
# Example format:
# MOMENTUM_GOOGLE_APPLICATION_CREDENTIALS_JSON='{"type":"service_account","project_id":"your-project-id","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}'
MOMENTUM_GOOGLE_APPLICATION_CREDENTIALS_JSON='{"type":"service_account","project_id":"your-project-id","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"..."}'

# =============================================================================
# GOOGLE AI / GENKIT CONFIGURATION
# =============================================================================
# Required for AI features: Gemini (text/vision), Imagen (images), Veo (videos)
# Get this from: Google AI Studio (https://aistudio.google.com/app/apikey)
# =============================================================================

# Google API Key for AI features (Gemini, Imagen, Veo)
MOMENTUM_GOOGLE_API_KEY="AIza..."

# Optional: Google Cloud Project ID (for Vertex AI features)
# Only needed if using Vertex AI instead of Google AI Studio
# MOMENTUM_GOOGLE_CLOUD_PROJECT="your-project-id"

# =============================================================================
# FIRECRAWL CONFIGURATION
# =============================================================================
# Required for Team Intelligence website crawling feature
# Get this from: Firecrawl Dashboard (https://www.firecrawl.dev/)
# =============================================================================

# Firecrawl API Key for website crawling
MOMENTUM_FIRECRAWL_API_KEY="fc-..."

# =============================================================================
# APPLICATION CONFIGURATION (Optional)
# =============================================================================
# These have sensible defaults but can be customized
# =============================================================================

# Site URL for email links and invitations (defaults to localhost:5000 in dev)
# Set this to your production domain when deploying
# MOMENTUM_NEXT_PUBLIC_SITE_URL="http://localhost:5000"

# Python Agent Service URL (defaults to http://127.0.0.1:8000)
# Only change if running Python service on different host/port
# MOMENTUM_PYTHON_AGENT_URL="http://127.0.0.1:8000"

# =============================================================================
# OPTIONAL SERVICES
# =============================================================================
# These are optional and only needed for specific features
# =============================================================================

# Firebase Email Extension Configuration (optional)
# Collection name for Trigger Email extension (defaults to 'mail')
# Only needed if using a custom collection name for the Firebase Trigger Email extension
# FIREBASE_EMAIL_COLLECTION="mail"

# Service Token for secure operations (optional, for production)
# Only needed in production environments
# MOMENTUM_SERVICE_TOKEN="..."

# =============================================================================
# NOTES
# =============================================================================
# 
# 1. All environment variables MUST use the MOMENTUM_ prefix
# 2. Variables starting with MOMENTUM_NEXT_PUBLIC_ are exposed to the browser
# 3. Never commit .env file to git (it contains secrets)
# 4. For production, use secure secret management (Replit Secrets, etc.)
# 5. The MOMENTUM_GOOGLE_APPLICATION_CREDENTIALS_JSON must be a single-line JSON string
#    - Remove all line breaks from the JSON
#    - Keep \n in the private_key field (they will be converted to actual newlines)
#    - Enclose the entire JSON in single quotes
#
# =============================================================================

